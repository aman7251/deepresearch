# deepresearch
An autonomous research assistant: you ask a hard question ("Compare the 2025 EU vs US AI regulation approaches"), it decomposes the task, spawns sub-agents (planner → web-search → reader → synthesizer → critic/verifier), builds a RAG index over the pages it fetches, and writes a cited report. 
