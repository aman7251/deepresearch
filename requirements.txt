# --- Core agent / orchestration ---
deepagents>=0.6,<0.7
langgraph>=0.2.50
langchain-core>=0.3.0
langchain-openai>=0.2.0

# --- LLM providers (OpenAI-compatible; Groq + Ollama both speak this) ---
openai>=1.40.0

# --- Web search + page reading ---
ddgs>=9.0,<10
httpx>=0.27.0
trafilatura>=1.12.0

# --- Ephemeral RAG ---
chromadb>=0.5.5
sentence-transformers>=3.0.0

# --- API + queue + worker ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
arq>=0.26.0
redis>=5.0.0

# --- UI ---
streamlit>=1.36.0

# --- Misc ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
tenacity>=8.4.0
